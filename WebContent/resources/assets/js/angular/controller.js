app.controller('dashboardController', function($scope) {
    $scope.headingTitle = "Photo Gallery Items";
});

app.controller('ordersController', function($scope) {
    $scope.headingTitle = "Contact Info";
});

app.controller('advertisementController', function($scope) {
    $scope.headingTitle = "Contact Info";
});

app.controller('settingsController', function($scope) {
    $scope.headingTitle = "Contact Info";
});

app.controller('messagesController', function($scope) {
    $scope.headingTitle = "Contact Info";
});

app.controller('referralController', function($scope) {
    $scope.headingTitle = "Contact Info";
});

app.controller('walletController', function($scope) {
    $scope.headingTitle = "Contact Info";
});
